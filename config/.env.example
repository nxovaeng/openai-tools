# API Server Configuration
HOST=0.0.0.0
PORT=8000

# Authentication
REQUIRE_AUTH=true
API_KEY=your-secure-api-key-here

# Deployment Mode
# - full: Full deployment with service restart
# - config_only: Only generate configs (for Docker)
# - container: All-in-one container mode
DEPLOYMENT_MODE=full

# Paths (adjust for your system)
XRAY_CONFIG_PATH=/etc/xray/config.json
NGINX_CONFIG_PATH=/etc/nginx/nginx.conf
NGINX_CONF_D_PATH=/etc/nginx/conf.d

# Xray Configuration
DEFAULT_XRAY_PORT=10000

# Logging
LOG_LEVEL=INFO
